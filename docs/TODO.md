# TODO

## 未着手

- [ ] Googleフォーム（日本語版・英語版）を実際に作成し、`src/app/config.ts` のURLを差し替える（要確認: フォーム項目は「場所・間違っている内容・正しい情報」の3つ程度）
- [ ] マップデータの現地検証（出口番号・店舗の実在・通路距離・階段/EV属性）
- [ ] 店舗・ノードデータの密度を上げる（現状は骨格のみ。特にホワイティのプチシャンゼリゼ、阪急三番街の店舗、ekimoの実テナント）
- [ ] 簡易マップ表示（ノード座標を使ったSVG俯瞰図）※静的表示のみ、追跡はしない
- [ ] 中国語・韓国語の追加（i18n構造は対応済み。`locales/`にzh/koを足し、`LocalizedText`の扱いを検討）
- [ ] GitHubリモート作成とpush（CIはpush後に初回実行される）
- [ ] デプロイ先の決定（GitHub Pages想定。`vite.config.ts` の `base` 設定が必要になる）

## 完了

- [x] Vite + React + TS スキャフォールドとツール整備（prettier / oxlint / vitest）
- [x] CI（GitHub Actions: typecheck / format / lint / test / build）
- [x] domain層: グラフ構築・Dijkstra経路探索（階段回避オプション付き）・位置推定・案内文生成 + テスト
- [x] 梅田地下全域の骨格マップデータ（ホワイティ・ディアモール・阪急三番街・ekimo・ドーチカ・駅通路）+ 整合性テスト
- [x] i18n（ja/en）
- [x] UI一式（位置確定・目的地選択・経路案内・迷ったボタン・報告リンク）+ フローテスト
- [x] PWA化（vite-plugin-pwa、ビルドでSW生成確認済み）

## 技術的負債・注意

- マップデータは2026-07-08時点の**現地未検証の下書き**。座標・距離・出口番号は概算。`dataVersion` を更新して管理する
- `locate()` のスコアリング定数（範囲・重み）は暫定値。実データが増えたら調整する
- 報告フォームURLはプレースホルダのまま（差し替え必須）

## 次回最初に着手するタスク

Googleフォーム作成とURL差し替え（5分で終わり、報告動線が本物になる）
